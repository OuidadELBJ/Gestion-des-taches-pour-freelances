<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- icône + texte quand l’item est sélectionné -->
    <item
        android:state_checked="true"
        android:color="@color/navActive" />

    <!-- icône + texte quand non sélectionné -->
    <item android:color="@color/navInactive" />
</selector>